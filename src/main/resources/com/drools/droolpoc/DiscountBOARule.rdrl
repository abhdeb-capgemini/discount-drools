package com.drools.droolpoc;

import java.lang.Number;

rule "DiscountBOARule"
	dialect "mvel"
	when
		request : InitialInputs( cardBankName == "BOA" )
	then
		request.setDiscountedPrice( request.productPrice - ((request.productPrice * 9 ) / 100) );
end
